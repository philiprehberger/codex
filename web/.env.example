SERVER_HOST=54.190.150.0
SERVER_USERNAME=ubuntu
SERVER_PRIVATE_KEY=/home/ubuntu/.ssh/ps4_new
SERVER_DEST_PATH=/var/www/codex-web
SERVER_PM2_PROCESS=codex-web

CODEX_API_INTERNAL_URL=http://127.0.0.1

# Don't set NEXT_PUBLIC_CODEX_API_HOST here. The dev value lives in
# .env.development (localhost) and the build value in .env.production
# (api.codex.philiprehberger.com). Both are committed to the repo;
# only this .env file holds machine-specific secrets + deploy paths.

# Google Analytics 4 measurement ID (optional; leave blank in forks)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
