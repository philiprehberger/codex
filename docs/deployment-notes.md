
  1. Laravel (scripts/deploy/laravel.sh) — ships the new controller, route, config key, observer change.
  2. Next.js (scripts/deploy/next.sh or cd web && npm run deploy) — ships the new component, type, page.